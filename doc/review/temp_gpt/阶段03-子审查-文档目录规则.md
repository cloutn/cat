# 阶段03-子审查-文档目录规则

子审查 agent：Lovelace `019e7e93-b237-7751-a848-8a889628840b`

## Critical

无。

## Warning

### W1 `.cursor` 规则与 AGENTS 覆盖优先级冲突

- commit：`8659e34`
- 文件：`.cursor/rules/cat代码风格.mdc:9`，关联 `AGENTS.md:60`
- 风险：`.cursor` 规则写“冲突时以更具体的本文件为准”，但 `AGENTS.md` 和本地 override 规则要求 `AGENTS.override.md` 优先。后续 agent 遇到个人硬规则冲突时可能错误覆盖本地约束。
- 建议：改成“与 override 冲突时以 override 为准；本文件只补充 cat 代码风格”。

### W2 新增 Markdown 文件名违反中文描述性规则

- commit：`d54198d` / `bcb508b`
- 文件：`doc/ecs/README.md:1`，`doc/todo/task2.md:1`
- 风险：新增 Markdown 使用 `README.md`、`task2.md` 这类英文/泛用名，违反本地规则“Markdown 文档必须中文描述性文件名”。
- 建议：重命名为中文描述名，例如 `doc/ecs/ECS文档索引.md`、`doc/todo/cat引擎开发计划-v2.md`，并同步更新相对链接。

### W3 文档 blob 不是合法 UTF-8

- commit：`bcb508b`
- 文件：`doc/architect/ai 给出的拆分方案.txt:1`，`doc/render/编译 compile build shaderc vulkan.txt:1`，`doc/todo/todo.txt:1`
- 风险：`git show` 读取出现乱码；AI/搜索/后续编辑容易基于错误内容判断，甚至二次保存造成永久损坏。
- 建议：将仍有价值的内容转成 UTF-8；若是临时 AI 输出，删除或迁移到明确的临时目录，并避免继续提交非 UTF-8 文档。

### W4 反思文档状态与提交清单不一致

- commit：`8e643f7`
- 文件：`doc/review/代码审查-反思-过度防御与冗余修改.md:4`、`:398`
- 风险：文档声称覆盖 `2026-05-30/31` 全部提交，但没有记录 `249d1e3`，且行动清单仍把多项已由 `249d1e3`/`8e643f7` 落地的修复写成待办，后续 agent 可能重复回滚或重复修改。
- 建议：把行动清单改成状态表，标明 `Done in 249d1e3/8e643f7` 与仍 open 的项，并把 `249d1e3` 补进提交清单。

## Suggestion

### S1 multi-agent-code-review skill 的 ignore 策略不完整

- commit：`9360343`
- 文件：`.gitignore:108`
- 风险：`.cursor/skills/multi-agent-code-review/` 只逐文件忽略当前已知文件，后续新增 prompt/example/README 仍可能被误提交；反过来如果这个 skill 要团队共享，当前规则又会隐藏核心文件。
- 建议：明确策略：个人 skill 就忽略整个 `/.cursor/skills/multi-agent-code-review/`；团队共享则移除这些 ignore 并提交完整 skill。

## 补充

- 子审查按提交树检查了 `d54198d`、`bcb508b`、`8e643f7` 的 Markdown 普通相对链接，未发现文件级断链。
- 残余风险：未做完整锚点 slug 校验，也未验证外链可达性。
