┌---------------------------┐
│ cat_utils                 │ 0 依赖
└-------------┬-------------┘
              │ public
┌-------------┴-------------┐
│ cat_math                  │ 仅依赖 cat_utils
└-------------┬-------------┘
              │ public
┌-------------┴-------------┐
│ cat_core                  │ 依赖 utils + math
└------┬------------┬-------┘
       │public      │public
┌------┴------┐ ┌---┴-----------┐
│cat_render_i │ │cat_resource   │ 抽象层，0 渲染/平台细节
│(interface)  │ │(IFileProvider)│
└--┬---┬------┘ └---┬-----------┘
   │   │public      │public
   │public          │
┌--┴---┴------┐ ┌---┴-----------┐
│cat_render_* │ │cat_scene      │ 场景=物体+相机+动画
│(gles/vk/d3d)│ │               │ 只认识抽象接口，不认识平台
└--┬----------┘ └---┬-----------┘
   │private         │public
   │       ┌--------┴-----------┐
   │       │cat_animation       │ 只操作关键帧与采样，不碰渲染
   │       └--------┬-----------┘
   │public          │public
┌--┴----------------┴-----------┐
│ cat_platform_*                │ Win32/X11/EGL/NS 等窗口实现
└--┬----------------------------┘
   │private（或运行时插件）
┌--┴----------------------------┐
│ cat_engine (顶层库/可执行文件) │ 把上面所有 .a 聚合成单 lib
└--------------------------------┘






推荐的层次化架构：

┌─────────────────────────────────────┐
│ cat_engine (应用层)                  │ ← testCat 链接的最终库
└─────────────────────────────────────┘
┌─────────────────┐ ┌─────────────────┐
│ cat_scene       │ │ cat_animation   │ ← 高级功能层
└─────────────────┘ └─────────────────┘
┌─────────────────┐ ┌─────────────────┐
│ cat_render_*    │ │ cat_platform_*  │ ← 后端实现层
│ (gles/vk/d3d)   │ │ (win32/linux)   │
└─────────────────┘ └─────────────────┘
┌─────────────────┐ ┌─────────────────┐
│ cat_render_i    │ │ cat_resource    │ ← 接口层
│ (interface)     │ │ (IFileProvider) │
└─────────────────┘ └─────────────────┘
┌─────────────────────────────────────┐
│ cat_core                            │ ← 核心层
└─────────────────────────────────────┘
┌─────────────────┐ ┌─────────────────┐
│ cat_math        │ │ cat_utils       │ ← 基础层
└─────────────────┘ └─────────────────┘