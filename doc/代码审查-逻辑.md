# cat ?????? ? ????

> ? agent?????
> ??????? C++ Vulkan/GLES ?????? UE?
> ????????????`cat/`?`catbase/`?`catui/`?`catvulkan/`?`catwindows/`???????

---

## ?? 1??? / ?? / ?? / ?? / Skin / Animation / Transform / KeyFrame

### Critical
- [??] `cat/object.h:77` ? `const Object* skinRoot() const { return skinRoot(); }` ? const ????? `skinRoot()`?? `this` ? `const Object*`????????????const ???? **???? / ???**????`return const_cast<Object*>(this)->skinRoot();`??? const ?????????
  - ????????? `const Object&` / `const Object*` ?? `skinRoot()` ????????? const-correctness ???
- [??] `cat/skin.h:24` ? `const Object* root() const { return root(); }` ???"const ? const = ????" ? **???? / ???**?? `object.h:77` ???????
  - ??????? `const Skin*` ???
- [??] `cat/object.cpp:323-328` ? `releaseObjectIDMap()` ? `delete s_objectIDMap`?**?????? NULL**???????? `_objectIDMap()`???? `Object` ???? `objectByID(...)`??`if (NULL == s_objectIDMap)` ???????????? ? **use-after-free**?
  - ???`delete s_objectIDMap; s_objectIDMap = NULL;`
  - ?????????/??????????Editor ?? reload?
- [??] `cat/skin.cpp:73-86` ? `generateJointMatrix` ??? `m_inverseBindMatrices[i]`?? `_loadMatrices` ? accessor ?? `r_32f / mat4` ? `view->size` ????**?? NULL**?line 23-32??????? `m_inverseBindMatrixCount` ??? `joints_count`?line 53???"count > 0 ? ptr == NULL"???? draw ? **??????**?
  - ????? NULL ?? count ?? 0??? generateJointMatrix ?????
  - ??????? IBM accessor ?? / ??? gltf ???????
- [??] `cat/animationChannel.cpp:137-141` ? `else if (time > frames[mid]->time())` ?????? `frames[mid + 1]`?`assert(mid < frames.size() - 1)` ?? Debug ???Release ?? `binary_search2` ??????????**????**??? line 119 ? `time %= lastFrame->time()`????? `time < lastFrame->time()`??? `binary_search2` ?"????????"? "lastFrame->time()==0" ???? size-1??????
  - ???`if (mid + 1 >= frames.size()) { _set(*frames[mid]); return; }`?
- [??] `cat/object.cpp:186-189` ? `loadSkin` ? `i` ???? `m_childs[i]` ? `node->children[i]`?? `loadNode` ? `node->children[i] == NULL` ?? **continue**?line 105-106??? push_back??? gltf ??? NULL child??????? ? **????????????**?
  - ????"?? child ??"??????? loadNode ??? NULL push ??????
  - ????????? child ??? gltf?

### Warning
- [??] `cat/object.cpp:165-175` ? `parentGlobalMatrixInverse()` ?????? `assert(false)` + ?? identity?Release ?????????????????????????/??/Gizmo ????????? `bool` ??????????????
- [??] `catbase/cat/transform.cpp:43-46` ? `Transform::setByMatrix(...)` ?? `m_move/m_scale/m_rotate` ?**?? `invalidate()`**???????? `m_changed==true` ???????????? `matrix()` ????????`m_changed==false`???? `setByMatrix`??? `matrix()` ???????
  - ??????? gltf ??????????????????"? draw ????????? transform"??????? stale matrix?
- [??] `cat/object.cpp:114-133` ? `Object::draw()` ? `globalMatrix()` ?????line 120 ? line 129?????????????????????? O(depth × childCount)???????? `Object::matrix()` ?? hook ?????????????????????????
- [??] `cat/object.cpp:330-342` ? `child(const char* objectName)` ?**??????**???? `child(int index)` ???????????????????"?? i ??? child" vs "?????"???????????????? `findChildByName(...)`?
- [??] `cat/object.cpp:345-363` ? `childByID(id, recursive)` ??/????? recursive ??? false????? `Scene::objectByID` ??? true?????? `recursive==false` ???????????????? ID???????????
- [??] `cat/scene.cpp:43-48` ? `for (size_t i = 0; i < scene.nodes_count; ++i) { ...->loadSkin(node, env); }` ??? `object` ??? NULL??? `getObjectByGltfNode`??????????????? `clearGltfNodeMap()` ???? node ????????
- [??] `cat/scene.cpp:95-99` ? `Scene::draw` ?? NULL ?? `m_objects[i]->draw(...)`?? `Scene::save`?line 65 ? NULL????????????? NULL `delete`?
- [??] `cat/animationChannel.cpp:146` ? `(after.time() - before.time())` ? `uint` ?????????? ? ?? NaN?? `after.time() < before.time()`?gltf ???????? ????????delta?0??????
- [??] `cat/animation.cpp:42-57` ? `Animation::update(double diff)` ? `m_time` ??? `diff` ??????? `static_cast<uint>(m_time)` ?? channel?channel ?? time ???**??**?`times[i]*1000`???? `diff` ???????????????????????/?????????????????? 1000x?
- [??] `cat/animationChannel.cpp:30-37` ? ????? `union { quaternion m_rotate; vector3 m_move; vector3 m_scale; }` **?????? clear()**?????????????????m_move????????????? `_resetTransform`/`_set` ?? type ???????????????????
- [??] `cat/animationChannel.cpp:53` ? `const int frameCount = timeAccessor->count;`?`count` ? `cgltf_size`(size_t)??? int ?????line 63 ?? `frameCount != frameAccessor->count` ????/????????????????
- [??] `cat/animation.cpp:33-38` ? `Animation::load` ?? `channel->target_node` ??? `env` ??????? `m_target` ??? -1???? `apply()` ? `objectByID(-1)` ?? NULL ??? return?Load ????? warning???????
- [??] `cat/skin.cpp:88-133` ? `Skin::root()` ? `m_joints[0]` ???"first joint" ????"?? hierarchy ?"?????????/?????????? gltf ???????fallback ???line 124-130?"depth ???????" ??? `m_joints[0]` ?? root????????

### Suggestion
- [??] `cat/object.cpp:75` ? `bool hasSkin() const { return skin() != NULL; }` ?? inline?????? OK ???
- [??] `cat/object.cpp:225-254` ? `setRotate/setScale/setMove` ?? "?? == ??? return" ??????`==` ?????????????????????????????????????"??"???????? bug????????????
- [??] `cat/skin.cpp:33-35` ? `new matrix[outputMatrixCount]` ? count==0 ????`delete[]` ?? OK ????
- [??] `free/scl/matrix.h:204-218` ? `mul(m1, m2)` ? `mul(m1, m2, m3)` ??? `*this` ???????`*this = *this * m1 * m2[ * m3]`???"?? `m1*m2`"????????? `mul_all` ?????????? public API ????????? `mul_chain(...)` ???? `*this = m1*m2[*m3]`?
- [??] `catbase/cat/transform.h:25` ? `invalidate()` ?? public??????????????????????????????????

---

## ?? 2??? / ??? / Env / Material / Light / Mesh / Primitive / cgltf_util / yaml / Shader / ShaderCache / Skybox / Terrain / Grid

### Critical
- [??] `cat/terrain.cpp:72` ? `m_primitive->setIndices(m_indices, 3, ELEM_TYPE_UINT32);` ???????? `3`??? `INDEX_COUNT`(=`(N-1)*(N-1)*2*3`)?**?? 1 ????**??? terrain ??????????"????"?????**??? debug ??**?
- [??] `cat/primitive.cpp:386-407` ? `Primitive::setAttrs` ? "attrCount > m_attrCount" ? realloc `m_attrs` ?**?? realloc `m_attrBufferIndices`**???? setAttrs(attrCount=2)???? setAttrs(attrCount=10)?line 402/406 ??**??? 2** ? `m_attrBufferIndices` ? 10 ? int ? **?????**?
  - ????????????????????dynamic mesh?editor ????
- [??] `cat/primitive.cpp:203` ? `void* _shader = isPick ? m_pickShader->shader(render) : m_shader->shader(m_render);` `m_pickShader` / `m_shader` ??? NULL ??**??????**?`setShader(...)` ???????? pickShader??? isPick=true ???
  - ???? NULL ????? setShader ????? pick?
- [??] `cat/grid.h:9` ?? `~Grid();`?? `cat/grid.cpp` **?????**????? `Grid g;` ? `delete grid;`?**????**?
- [??] `cat/env.cpp:56-61` `Env::getDefaultMaterial()` ? ???????? default?**???? new ??? Material**???? `cat/primitive.cpp:187` ??? m_material????? Primitive ?? delete??????????????????? "default = singleton" ?? delete ? double-delete?
  - ????? `createDefaultMaterial()` ???????

### Warning
- [??] `cat/primitive.cpp:436-437` ? `setVertices(verticesList ...)` ?? `m_deviceVertexBuffers = new void*[m_attrCount]`?**?????**????? ? ?? + GPU buffer ????
- [??] `cat/primitive.cpp:470` ? `if (m_deviceVertexBuffers[0] <= 0)` ? `m_deviceVertexBuffers == NULL` ?????? NULL?`updateVertices` ? `setVertices` ???????
- [??] `cat/primitive.cpp:80` ? `int attrOffsets[1024] = { 0 };` ?? 4KB ?????gltf attr ? > 1023 ?????? gltf ??? 1024 ? attr??????
- [??] `cat/primitive.cpp:617-618` ? `vertexPositions(vector3* positions, ...)` ? `positions[0].value_ptr()` + `m_vertexCount * sizeof(vector3)` ??????`scl::vector3` ? 3 ? float ? POD??????/???? padding???? `sizeof(vector3) > 12`??????? `vertexAttrs` ? 12 ????**???**?position ?????????? `3 * sizeof(float) * count`?
- [??] `cat/primitive.cpp:84` ? `vertexCount = primitive.attributes[0].data->count;` ???? attr count ???gltf spec ????????????
- [??] `cat/primitive.cpp:361-369` `setTexture(filename)` ? ????? filename ?????**??? new Material()?filename ????**???????????????????
- [??] `catbase/cat/cgltf_util.c:66-75` `cgltf_primitive_has_attr` ? `_strnicmp` ?**??**????? "POSITION" ? "POSITIONS" ????gltf ????????????? "TEXCOORD" ???? "TEXCOORD_0"?????????? "JOINTS" ???? "JOINTS_0"?OK?? `shaderCache.cpp:141` ??? **?? "joints"**??? 6???? `"JOINT" ???? attr` ?? gltf ???? JOINTS_n?????????? "JOINT_INVALID" ?? attr ????
- [??] `catbase/cat/cgltf_util.c:4-8` `cgltf_get_accessor_buffer` ?? NULL?accessor / buffer_view / buffer ??? NULL ??????????????? `cat/animationChannel.cpp:69-70`?`cat/primitive.cpp:172`??
- [??] `catbase/cat/cgltf_util.c:71` `_strnicmp` ? MS-only API???????Vulkan/GLES + EGL Window??????? `strncasecmp`???????
- [??] `cat/shader.cpp:54-57` ? `_loadfile` ????? `return false;`???????? `char*`?`false` ?? ? `(char*)0`?????? NULL ???????? `render->createShader(NULL, ...)`?**????**?
- [??] `cat/shader.cpp:25-32` ? `Shader::shader(render)` ? `m_render = render`??? `render->releaseShader(m_deviceShader)`??????? deviceShader ???? m_render ????? render ? release ???????? assert ?? render??? m_render ??? release?
- [??] `catbase/cat/camera.cpp:115-131` `orbit_right` / `orbit_up`?line 134-152??? `m_up = vector3::cross(right, front);` **? normalize**?? `rotate(...)` line 109 ? normalize??? orbit ? m_up ?????view matrix ?????????
- [??] `catbase/cat/camera.cpp:86-92` `Camera::move(d)` ? `_front()` ????? `target = position + lookat`?**???? target-position ????? 1**????? orbit ??? 5?move ??? 1??? orbit ????????? "???" ????
- [??] `cat/env.cpp:94-107` `Env::releaseTextureFile(NULL)` ?? `pTextureFile->name` ??? ? ??? NULL ????? `Env::releaseTextureFile(&TextureFile::empty())`??? line 81 ?? fallback??? empty ? name ? find????? silent return?counter ??????
- [??] `cat/env.cpp:148-153` `registerPickPrimitive` ? `m_pickPrimitives.size()` ? id?array ?? 1024 ?????? `push_back` UB / assert?Pick ??????????????
- [??] `cat/light.cpp:5-9` `Light::Light()` ???? `m_transform`?**`m_color` ????** ? ????? float ??`color()` ????????
- [??] `catbase/cat/transform.cpp:22-33` `mul(scale_mat) ? mul(rotate) ? mul(move)` ?? `S*R*T`??? `scl::matrix::move` ? dx/dy/dz ??? 4 ??`x4,y4,z4`??? `mul` ??? `result[r][c]=? this[r][k]*other[k][c]`????scl ?**??????v_row * M?**????????? `transpose` ??? shader?shader ?????????????/???????? shader ? / IRender ????????????
- [??] `cat/animationChannel.cpp:30-37` ? `catbase/cat/keyFrame.cpp:12-17` ?? union ?? clear() ????????? clear() ?????????????????????"???"??????????? type ????????????????

### Suggestion
- [??] `catbase/cat/camera.cpp:12-26` ??????????????**???**?? fov... ? ortho/zRange/orthoRefPosition????? `-Wreorder` ??????????
- [??] `cat/shaderCache.cpp:23-66` cache key ? macros ?**????**??????? macro ????????? key???????? macros ? cache ? sort by name?
- [??] `cat/shaderCache.cpp:111-117` `getPickShader` ???? input shader ? macros ? `PICK`?? `COLOR/TEXTURE`??? user shader ??? `COLOR`/`TEXTURE` ?????????pick ???? main ???????????
- [??] `cat/material.cpp:14` ctor ???? `m_env`??? `release()` ?? `m_textureFile != NULL` ??? m_env?????????????????????????+?????????? init list ?? NULL?
- [??] `catbase/cat/yaml.h:163-176` `node_iterator::m_childID` ??? `size_t` ???? `int`?`-1` ?? ? `SIZE_MAX`?`assert(m_childID != -1)` ?? size_t vs int ???????? int?
- [??] `cat/terrain.cpp:24-25` `x = row - TERRAIN_SIZE/2; y = -col + TERRAIN_SIZE/2;` int ??????????4 ?? -2..1???? `(TERRAIN_SIZE - 1) / 2.0f` ???
- [??] `catbase/cat/box.h:11` ?? `Box(_min, _max)` ? `ensureMinMax()` ?????????????? `m_min(_min), m_max(_max)` ????OK?? `set(_min, _max)` ????????? `setMinDirect` / `setMaxDirect` **??** ensureMinMax???"????" OK?

